name: C/C++ CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: self-hosted

    steps:
    - name: clone
      run: git clone --recurse-submodules https://github.com/Burfedd/FunnyBoi.git
    - name: cmake
      run: cmake .
    - name: make
      run: make
    - name: run
      run: ./FunnyBoi/Funnyboi
